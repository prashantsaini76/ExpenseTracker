import React from 'react'

function Card({children}) {
  return (
   

        <div className='w-[100vw] bg-white rounded-lg overflow-hidden flex flex-col font-sans]'>
            {children}
        </div>
      
    
  )
}

export default Card
